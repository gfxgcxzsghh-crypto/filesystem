# FiveM 成品端 · 完美置换教程（宝塔终端版）

> 给管理员看的傻瓜教程。**全程在宝塔【终端】里粘命令**（宝塔左侧菜单 →「终端」）。
> 核心原则：**危险操作全部"改名保留"，不删除**，任何一步翻车都能 5 分钟滚回现在能跑的样子。
> ⚠️ 遇到**红字报错**别硬闯，截图发老板问 Claude。

---

## 名词对照（先认清）
- **旧端**：现在在跑的服务端，目录 `/opt/jiutian_new`
- **新端**：要换上去的成品整合包，先传到 `/opt/new_server`
- **宝塔终端**：宝塔面板左侧「终端」，在里面粘命令、按回车执行

---

## 第 0 步：确认两个备份都在（没有就别往下！）

粘这条：

```
echo "== 服务端备份 =="; ls -lh /opt/jiutian_OLD_*.tar.gz 2>/dev/null || echo "没有！"; echo "== 数据库备份 =="; ls -lh /opt/db_backup_*.sql 2>/dev/null || echo "没有！"
```

**两个都要看到文件 + 大小**（不是"没有！"）。缺任何一个，停下，先补备份（问老板要备份命令）。这是保命的，缺了别换。

---

## 第 1 步：把新端放到临时目录（别直接怼进旧端！）

1. 宝塔【文件】→ 进到 `/opt` → 新建文件夹 `new_server`
2. 把新端整合包**上传**进 `/opt/new_server`
3. 如果是 `.zip`/`.rar`：**右键 → 解压**，然后**删掉压缩包**
4. 回终端确认结构：

```
echo "== 新端里有啥 =="; ls -la /opt/new_server; echo "== 找新端的 resources =="; find /opt/new_server -maxdepth 3 -type d -name resources; echo "== 找新端自带的数据库 sql =="; find /opt/new_server -maxdepth 3 -iname "*.sql"
```

**记下三个结果**（后面要用）：
- 新端 `resources` 的完整路径（下面叫「新resources路径」）
- 有没有自带 `.sql` 数据库文件（有就记下路径）
- 有没有自带 `server.cfg`

> 如果 `resources` 没找到，说明新端结构特殊或没解压好，截图问老板。

---

## 第 2 步：扫新端后门（装之前必扫！）

```
curl -L -o /opt/fivem_scan.py "https://raw.githubusercontent.com/gfxgcxzsghh-crypto/filesystem/claude/new-session-3tgjd3/fivem_scan.py" && python3 /opt/fivem_scan.py /opt/new_server
```

看结果：
- 🔴 **严重/高危**（尤其 `load`+`base64` 一起出现）→ **别换这个端！** 有后门，截图发老板
- 🟡 中/低 → 正常，点开看看和功能相关就行
- 全绿 → 放心往下

---

## 第 3 步：停服

在 **txAdmin 网页面板**里点 **Stop / 停止服务器**（别边跑边换）。
停好后，下面的替换才安全。

---

## 第 4 步：替换 resources（旧的改名留着，能回滚）

**把下面命令里的「新resources路径」换成第 1 步记下的真实路径**，再粘：

```
cd /opt/jiutian_new
mv resources resources_OLD
cp -r 新resources路径 ./resources
chown -R www:www ./resources
echo "== 换好了,现在 resources 里有 =="; ls ./resources | head
```

> 例：如果新端 resources 在 `/opt/new_server/resources`，第三行就是
> `cp -r /opt/new_server/resources ./resources`
> 旧的资源全在 `resources_OLD` 里没删，翻车能拿回来。

---

## 第 5 步：换 server.cfg —— 最关键的"接线"

新端自带的 `server.cfg` 里，有 3 样是**别人的**，必须换成**你这台的**，否则服起不来：

**① 先备份旧 cfg，并打印出你这台的关键信息：**

```
cd /opt/jiutian_new
cp server.cfg server.cfg.OLD
echo "===== 你这台的关键配置(记下来) ====="
grep -iE "sv_licenseKey|mysql_connection_string|endpoint_add|sv_hostname" server.cfg.OLD
```

把打印出来的这几行**记下来**（尤其 `sv_licenseKey` 和 `mysql_connection_string`）。

**② 用新端的 cfg 覆盖，但改回你的信息：**

- 如果新端**自带 server.cfg**：宝塔【文件】里打开新端的 cfg，把里面的
  - `sv_licenseKey` → 改成你上面记下的（license 是绑这个服的）
  - `mysql_connection_string` → 改成你上面记下的（连你这台数据库）
  - `endpoint_add_tcp/udp` 端口 → 保持你原来的
  然后把它存成 `/opt/jiutian_new/server.cfg`
- 如果新端**没带 cfg**：那就继续用旧的 `server.cfg`，只需按新端说明，把新资源的 `ensure xxx` 加进去

> 这步是换端最容易出错的地方。改完拿不准，把 cfg 内容截图发老板核对再启动。

---

## 第 6 步：导入新端数据库（⚠️ 会覆盖玩家数据）

**先想清楚：**
- 新端一般要用**它自带的数据库结构**（表和旧的不一样），所以通常要导入新 sql，**老玩家数据会清空**（重新开档）
- 想保留老玩家数据：这步先别做，直接第 7 步试起服；起不来/报表错误，再回来导新库

**要导入的话**（把「新端sql路径」换成第 1 步找到的真实 .sql 路径）：

```
curl -L -o /opt/db_import.py "https://raw.githubusercontent.com/gfxgcxzsghh-crypto/filesystem/claude/new-session-3tgjd3/db_import.py" && python3 /opt/db_import.py 新端sql路径
```

> 这个脚本会自动读连接串、把新 sql 导进你这台数据库。看到 `==导入完成==` 就好。
> 报错就截图发老板。

---

## 第 7 步：启动 + 进游戏验证

1. txAdmin 网页里点 **Start / 启动**
2. 看 txAdmin 控制台有没有**红字报错**（有就截图发老板）
3. **进游戏跑一圈**：能进服、能出生、菜单/背包/车都正常 = 换成功 🎉

---

## 第 8 步：稳了再清理（别急，留几天）

确认几天没问题，再删这些占地方的旧东西：

```
cd /opt/jiutian_new && rm -rf resources_OLD server.cfg.OLD
```

（备份包 `/opt/jiutian_OLD_*.tar.gz` 和 `/opt/db_backup_*.sql` 可以多留一阵）

---

## 🔙 翻车了？一键滚回换之前

只要前面按"改名保留"做的，随时能回：

```
cd /opt/jiutian_new
rm -rf resources && mv resources_OLD resources
cp server.cfg.OLD server.cfg
chown -R www:www resources
echo "== 已还原 resources 和 cfg =="
```

数据库如果导过新库、要还原老数据：

```
python3 /opt/db_import.py $(ls -t /opt/db_backup_*.sql | head -1)
```

然后 txAdmin 重启，就回到换之前能跑的样子了。

---

## 一句话总结
备份 → 传新端到 new_server → 扫后门 → 停服 → 换 resources（旧的改名）→ 改 cfg 接线（license/数据库/端口用旧的）→ 导数据库 → 启动验证 → 稳了再清理。
**每一步都能回滚，拿不准就截图问老板，别硬闯。**
